// Funções: neste momento, irá nos ajudar a explicar o que o código está fazendo (através do nome explicativo) e tornar mais fácil a reutilização de códigos.

// Exemplo: lista de frases

// function declaration | function statement

function createPhrases() {
    console.log('Comer é bom')
    console.log('Dormir é ótimo')       
    console.log('Estudar também é bom') // ao rodar este código, não acontecerá nada aparantemente, pois o JS está guardando este bloco de código p/ uso posterior.
}

// como executar a função?
// rodar, chamar, incovar... Execute, run, call, invoke

createPhrases() // esta parte é a necessária p/ que os comandos do bloco sejam executados... podendo ser executado quantas vezes precisar
createPhrases()
createPhrases()
createPhrases()





function printanNome(nomeCompleto) {
    console.log(nomeCompleto)
}

printanNome("Alexandre");


function nomeDaFunção(Parametros) {
    //corpo da função
}

nomeDaFunção(Parametros)

