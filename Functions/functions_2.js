// podemos declarar funções dentro de variáveis

// function expression | function anonymous

const sum = function(number1, number2, number3) {   // dentros destes parênteses estão os parameteres
    console.log(number3 * number1)
}

sum(4,5,20) // dentro destes parênteses estão os arguments

// pode ser feito contas de +, -, * e /









const soma = function (numero1, numero2) {
    return numero1 + numero2;
}

soma(1,2);


const nomeDoQueVcQuerFazer = function(parametros) {
    //corpo da função
}

//A variável nomeDoQueVcQuerFazer vira uma função
//A variavel precisa ser chamada como função para aceitar a função anônima
nomeDoQueVcQuerFazer(parametros)
